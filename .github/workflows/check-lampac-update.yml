name: Check Lampac update

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - name: Check upstream Lampac image
        run: |
          docker pull ghcr.io/lampac-nextgen/lampac:latest
